# MEMORY

轻量长期记忆 — 仓库：scsun1978/github-bot

说明
- 本文件保存长期稳定且需长期检索的信息；当日/过程性信息放在 `memory/YYYY-MM-DD.md`（每日记录）。
- 遵循 `AGENTS.md` 中定义的分层与 Promotion 规则：只把真正长期且需重复使用的结论写入本文件。
- 保持精简、可扫描：每条记录包含主题、简短描述、来源/链接、最后更新时间。

核心条目（精炼）

- 仓库目标
  - 把本仓库建设为基于 GitHub 的个人 AI 工作空间：在 Copilot 网页版与后续对话中维持长期驻留角色，且以“文件为唯一记忆来源”。
  - 最后更新时间：2026-04-28

- Agent 行为约束与偏好（摘要）
  - 文件即记忆���重要信息必须写入仓库文件（AGENTS.md、MEMORY.md 或 memory/ 下文件）。
  - 先写每日，后提炼：不确定时先写 `memory/YYYY-MM-DD.md`（append-only），经确认再迁移到 `MEMORY.md`（参见 AGENTS.md 的 Promotion 规则）。
  - 任务记录原则：任务以轻量清单记录在当日日志的 Tasks 区块，完成后勾选并附产物链接（commit/PR/文件路径）。
  - 文档语言优先中文（zh-CN）；必要时附英文摘要。
  - 不在仓库保存明文敏感凭证；在本文件仅记录“如何安全访问/获取”的方法，不包含凭证明文。

- 活动摘要（当前活跃项目 / 关注点）
  - AGENTS.md 已由用户定稿并提交，本次已同步并优化 MEMORY 与 memory/README.md（以匹配 AGENTS.md 的规则）。
  - 初期待办（最小集合）
    1. 在 README 中加入“如何与 agent 交互”简短段落（下一步）。
    2. 创建 Issue/PR 模板（task / bug）。
    3. 可选：实现自动化脚本（每日日志生成、记忆迁移、定期复核）。

重要链接（仓库内）
- AGENTS.md: ./AGENTS.md
- memory/ 目录: ./memory/

维护说明
- 修改本文件时，请在当日日志中记录修改来源与原因，并更新本条目的“最后更新时间”。
- 若某一主题条目变得庞大或专题化，请拆分至 `memory/persistent/<topic>.md` 并在本文件中加入索引条目。

最后更新：2026-04-28
